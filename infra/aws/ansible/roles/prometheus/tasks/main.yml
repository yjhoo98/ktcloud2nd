---
- name: Wait for operator worker nodes to become ready
  command: kubectl wait node --selector=role=operator-worker --for=condition=Ready --timeout=60s
  environment:
    KUBECONFIG: /etc/rancher/k3s/k3s.yaml
  run_once: true
  ignore_errors: yes

- name: Add prometheus-community Helm repository
  command: helm repo add prometheus-community https://prometheus-community.github.io/helm-charts --force-update
  environment:
    KUBECONFIG: /etc/rancher/k3s/k3s.yaml
  run_once: true

- name: Update Helm repositories
  command: helm repo update
  environment:
    KUBECONFIG: /etc/rancher/k3s/k3s.yaml
  run_once: true

- name: Ensure operator-app namespace exists
  shell: kubectl create namespace operator-app --dry-run=client -o yaml | kubectl apply -f -
  environment:
    KUBECONFIG: /etc/rancher/k3s/k3s.yaml
  run_once: true

- name: Prepare kube-prometheus-stack asset directory
  file:
    path: /tmp/kube-prometheus-stack/dashboards
    state: directory
    mode: "0755"
  run_once: true

- name: Upload kube-prometheus-stack values
  copy:
    src: kube-prometheus-stack-values.yaml
    dest: /tmp/kube-prometheus-stack/kube-prometheus-stack-values.yaml
    mode: "0644"
  run_once: true

- name: Upload provisioned Grafana dashboard
  copy:
    src: grafana-dashboards/k3s-infra-overview.json
    dest: /tmp/kube-prometheus-stack/dashboards/k3s-infra-overview.json
    mode: "0644"
  run_once: true

- name: Render custom Grafana dashboard ConfigMap
  template:
    src: grafana-dashboard-k3s-infra-overview-configmap.yaml.j2
    dest: /tmp/kube-prometheus-stack/grafana-dashboard-k3s-infra-overview-configmap.yaml
    mode: "0644"
  run_once: true

- name: Apply custom Grafana dashboard ConfigMap for sidecar discovery
  command: kubectl apply -f /tmp/kube-prometheus-stack/grafana-dashboard-k3s-infra-overview-configmap.yaml
  environment:
    KUBECONFIG: /etc/rancher/k3s/k3s.yaml
  run_once: true

- name: Check Grafana deployment status
  shell: kubectl get deployment kube-prometheus-stack-grafana --namespace operator-app 2>/dev/null && echo "exists" || echo "not-found"
  environment:
    KUBECONFIG: /etc/rancher/k3s/k3s.yaml
  run_once: true
  register: grafana_deployment_status
  failed_when: false
  changed_when: false

- name: Check kube-prometheus-stack release status
  shell: >
    helm status kube-prometheus-stack --namespace operator-app --output json 2>/dev/null
    | python3 -c "import sys,json; print(json.load(sys.stdin)['info']['status'])"
    2>/dev/null || echo "not-found"
  environment:
    KUBECONFIG: /etc/rancher/k3s/k3s.yaml
  run_once: true
  register: prometheus_release_status
  failed_when: false
  changed_when: false

- name: Check Prometheus StatefulSet status
  shell: kubectl get statefulset prometheus-kube-prometheus-stack-prometheus --namespace operator-app 2>/dev/null && echo "exists" || echo "not-found"
  environment:
    KUBECONFIG: /etc/rancher/k3s/k3s.yaml
  run_once: true
  register: prometheus_statefulset_status
  failed_when: false
  changed_when: false

- name: Uninstall broken kube-prometheus-stack release when needed
  command: helm uninstall kube-prometheus-stack --namespace operator-app
  environment:
    KUBECONFIG: /etc/rancher/k3s/k3s.yaml
  run_once: true
  when: prometheus_release_status.stdout in ["failed", "pending-install", "pending-upgrade"] or (prometheus_release_status.stdout == "deployed" and prometheus_statefulset_status.stdout == "not-found")

- name: Deploy kube-prometheus-stack with Grafana provisioning
  command: >
    helm upgrade --install kube-prometheus-stack prometheus-community/kube-prometheus-stack
    --namespace operator-app
    --create-namespace
    --timeout 15m
    --values /tmp/kube-prometheus-stack/kube-prometheus-stack-values.yaml
    --set prometheusOperator.admissionWebhooks.enabled=false
    --set prometheusOperator.admissionWebhooks.patch.enabled=false
    --set prometheusOperator.tls.enabled=false
    --set prometheusOperator.tolerations[0].key=nodetype
    --set prometheusOperator.tolerations[0].operator=Equal
    --set prometheusOperator.tolerations[0].value=operator
    --set prometheusOperator.tolerations[0].effect=NoSchedule
    --set prometheusOperator.nodeSelector.role=operator-worker
    --set grafana.adminPassword={{ vault_grafana_admin_password }}
    --set grafana.env.GF_SERVER_ROOT_URL="%(protocol)s://%(domain)s/grafana/"
    --set grafana.env.GF_SERVER_SERVE_FROM_SUB_PATH=true
    --set grafana.env.GF_SECURITY_ALLOW_EMBEDDING=true
    --set grafana.env.GF_AUTH_ANONYMOUS_ENABLED=true
    --set grafana.env.GF_AUTH_ANONYMOUS_ORG_ROLE=Viewer
    --set grafana.sidecar.dashboards.enabled=true
    --set grafana.sidecar.dashboards.label=grafana_dashboard
    --set grafana.tolerations[0].key=nodetype
    --set grafana.tolerations[0].operator=Equal
    --set grafana.tolerations[0].value=operator
    --set grafana.tolerations[0].effect=NoSchedule
    --set grafana.nodeSelector.role=operator-worker
    --set prometheus.prometheusSpec.tolerations[0].key=nodetype
    --set prometheus.prometheusSpec.tolerations[0].operator=Equal
    --set prometheus.prometheusSpec.tolerations[0].value=operator
    --set prometheus.prometheusSpec.tolerations[0].effect=NoSchedule
    --set prometheus.prometheusSpec.nodeSelector.role=operator-worker
    --set alertmanager.alertmanagerSpec.tolerations[0].key=nodetype
    --set alertmanager.alertmanagerSpec.tolerations[0].operator=Equal
    --set alertmanager.alertmanagerSpec.tolerations[0].value=operator
    --set alertmanager.alertmanagerSpec.tolerations[0].effect=NoSchedule
    --set alertmanager.alertmanagerSpec.nodeSelector.role=operator-worker
  environment:
    KUBECONFIG: /etc/rancher/k3s/k3s.yaml
  run_once: true

- name: Wait for Grafana rollout
  command: kubectl rollout status deployment/kube-prometheus-stack-grafana --namespace operator-app --timeout=300s
  environment:
    KUBECONFIG: /etc/rancher/k3s/k3s.yaml
  run_once: true

- name: Restart Grafana to reload provisioned dashboards
  command: kubectl rollout restart deployment/kube-prometheus-stack-grafana --namespace operator-app
  environment:
    KUBECONFIG: /etc/rancher/k3s/k3s.yaml
  run_once: true
  when: grafana_deployment_status.stdout == "exists"

- name: Wait for Grafana rollout after restart
  command: kubectl rollout status deployment/kube-prometheus-stack-grafana --namespace operator-app --timeout=300s
  environment:
    KUBECONFIG: /etc/rancher/k3s/k3s.yaml
  run_once: true
  when: grafana_deployment_status.stdout == "exists"

- name: Wait for Prometheus StatefulSet to report replicas
  command: kubectl wait statefulset/prometheus-kube-prometheus-stack-prometheus --namespace operator-app --for=jsonpath='{.status.replicas}' --timeout=120s
  environment:
    KUBECONFIG: /etc/rancher/k3s/k3s.yaml
  run_once: true
  failed_when: false

- name: Wait for Prometheus rollout
  command: kubectl rollout status statefulset/prometheus-kube-prometheus-stack-prometheus --namespace operator-app --timeout=300s
  environment:
    KUBECONFIG: /etc/rancher/k3s/k3s.yaml
  run_once: true

- name: Linkerd 컨트롤 플레인 PodMonitor 등록
  shell: |
    kubectl apply -f - <<EOF
    apiVersion: monitoring.coreos.com/v1
    kind: PodMonitor
    metadata:
      name: linkerd-controller
      namespace: operator-app
      labels:
        release: kube-prometheus-stack
    spec:
      namespaceSelector:
        matchNames:
        - linkerd
        - linkerd-viz
      selector:
        matchLabels: {}
      podMetricsEndpoints:
      - port: admin-http
        path: /metrics
    EOF
  environment:
    KUBECONFIG: /etc/rancher/k3s/k3s.yaml
  run_once: true

- name: Linkerd 데이터플레인 프록시 PodMonitor 등록
  shell: |
    kubectl apply -f - <<EOF
    apiVersion: monitoring.coreos.com/v1
    kind: PodMonitor
    metadata:
      name: linkerd-proxy
      namespace: operator-app
      labels:
        release: kube-prometheus-stack
    spec:
      namespaceSelector:
        any: true
      selector:
        matchExpressions:
        - key: linkerd.io/control-plane-ns
          operator: Exists
      podMetricsEndpoints:
      - port: linkerd-admin
        path: /metrics
    EOF
  environment:
    KUBECONFIG: /etc/rancher/k3s/k3s.yaml
  run_once: true

- name: Linkerd Grafana 대시보드 추가
  command: >
    helm upgrade kube-prometheus-stack prometheus-community/kube-prometheus-stack
    --namespace operator-app
    --reuse-values
    --set "grafana.dashboards.default.linkerd-top-line.gnetId=11868"
    --set "grafana.dashboards.default.linkerd-top-line.revision=6"
    --set "grafana.dashboards.default.linkerd-top-line.datasource=Prometheus"
    --set "grafana.dashboards.default.linkerd-health.gnetId=11869"
    --set "grafana.dashboards.default.linkerd-health.revision=3"
    --set "grafana.dashboards.default.linkerd-health.datasource=Prometheus"
    --set "grafana.dashboards.default.linkerd-deployment.gnetId=11870"
    --set "grafana.dashboards.default.linkerd-deployment.revision=6"
    --set "grafana.dashboards.default.linkerd-deployment.datasource=Prometheus"
  environment:
    KUBECONFIG: /etc/rancher/k3s/k3s.yaml
  run_once: true
